# NVIDIA — restore SM com CNPJ de teste

Script usado no PC piloto `7E437B04` (NIVIDIA TESTE).

`restaurar-sm-cnpj-teste.ps1` restaura o dump SM do Leo e grava **`29366969000162`** em `empresas.emp_004` **antes** de qualquer abertura do SOS. Monitor fica off pra venda de teste não ir pra loja SM.
